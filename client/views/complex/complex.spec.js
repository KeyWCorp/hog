'use strict';

describe('Controller: ComplexCtrl', function () {

  beforeEach(module('hog'));

  var ComplexCtrl;

  beforeEach(inject(function ($controller) {
    ComplexCtrl = $controller('ComplexCtrl', {});
  }));

  it('should ...', function () {
    expect(1).toBe(1);
  });

});
